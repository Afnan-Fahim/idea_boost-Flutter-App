name: ideaboost
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+2

environment:
  sdk: ^3.8.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  cloud_firestore: ^6.1.0
  crypto: ^3.0.3
 
  equatable: ^2.0.7
  uuid: ^4.0.0
  dio: ^5.4.0

  
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  firebase_storage: 13.0.4
  firebase_remote_config: ^6.1.2
  image_picker: ^1.0.7
  flutter_image_compress: ^2.2.0
  firebase_crashlytics: ^5.0.5
 
  connectivity_plus: ^7.0.0
  flutter_native_splash: ^2.4.7
  google_mobile_ads: ^6.0.0
  easy_localization: ^3.0.0
  auto_size_text: ^3.0.0
  google_sign_in: ^7.2.0
  sign_in_with_apple: ^7.0.1
  device_info_plus: ^11.2.0
  http: ^1.2.2
  url_launcher: ^6.3.2
  flutter_svg: ^2.0.0
  app_links: ^6.3.2
  #easy_localization_loader: ^1.0.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/logo.png"


  flutter_lints: ^5.0.0

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/apple.png
    - assets/splash.png
    - assets/LookSplash.png
    - assets/google.png
    - assets/ideas_en.json
    - assets/ideas_ru.json
    - assets/ideas_uz.json
    - assets/ideas_es.json
    - assets/ideas_pt.json
    - assets/ideas_ar.json
    - assets/ideas_hi.json
    - assets/ideas_fr.json
    - assets/ideas_de.json
    - assets/ideas_vi.json
    - assets/ideas_id.json
    - assets/ideas_ms.json
    - assets/youth_en.json
    - assets/youth_ru.json
    - assets/youth_uz.json
    - assets/youth_es.json
    - assets/youth_pt.json
    - assets/youth_ar.json
    - assets/youth_hi.json
    - assets/youth_fr.json
    - assets/youth_de.json
    - assets/youth_vi.json
    - assets/youth_id.json
    - assets/youth_ms.json
    - assets/seasonal_en.json
    - assets/seasonal_ru.json
    - assets/seasonal_uz.json
    - assets/seasonal_es.json
    - assets/seasonal_pt.json
    - assets/seasonal_ar.json
    - assets/seasonal_hi.json
    - assets/seasonal_fr.json
    - assets/seasonal_de.json
    - assets/seasonal_vi.json
    - assets/seasonal_id.json
    - assets/seasonal_ms.json
    - assets/logo.png
    - assets/app.png
    - assets/lang/
    - assets/icons/
    - assets/onboarding/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
flutter_native_splash:
  color: "#000000"      # Pure black background — no app icon during splash transition
  # Removed: image: assets/app.png (was causing default icon to show on some devices)
  android: true
  ios: true
  android_gravity: center
  ios_content_mode: scaleAspectFit
  fullscreen: true
  web: false